Claude(ブラウザ版)のchat内容を、新規chat作成時に引き継ぎたい。エンジンをSonnetからHaikuに変更する時等に使う。

処理経緯を要約して引継。データは手動で移管するので、テキストのみ。引継に際して、データの受け渡しが必要な場合はその旨のメッセージが出る。

引き継ぎ作業自体は手動で行う。処理経緯を要約して引継用に渡せるテキストを出力する＋引継に際して、データの受け渡しが必要な場合はその旨のメッセージが出る。　　という拡張機能を作成せよ。
